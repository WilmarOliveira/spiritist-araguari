import { ContainerFooter, TitleFooter } from "./styles"

export const Footer = () => {
    return (
        <ContainerFooter>
            <TitleFooter>Araguari Espírita</TitleFooter>
        </ContainerFooter>
    )
}